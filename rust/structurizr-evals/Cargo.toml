[package]
name = "structurizr-evals"
version = "0.1.1"
edition = "2021"
publish = false

[dependencies]
structurizr-model = { path = "../structurizr-model" }
structurizr-query = { path = "../structurizr-query" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
structurizr-dsl = { path = "../structurizr-dsl" }
